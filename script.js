function bttnAlert() {
     // Fetch the value of the email input field
     let emailValue = document.getElementById('email').value;

     // Display the email address in the alert
     alert(`If this company was real, your email (${emailValue}) would have been submitted successfully :)`);
}

function searchAlert(){
    alert("This is a magnifying glass... Hope you weren't expecting something else XD");
}